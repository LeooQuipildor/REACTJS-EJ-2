import React from 'react';

const Titulo = ({nuevoTitulo}) => {
    return (
        <div>
            <h1>{nuevoTitulo}</h1>
            <button>Hola</button>
        </div>
    );
};

export default Titulo;